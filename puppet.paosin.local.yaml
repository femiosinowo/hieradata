firewall::desc: '200 allow multiple ports to be opened for puppet stuff'
firewall::ports: [443,61613,8140]
firewall::proto: tcp
firewall::action: accept


firewall::desc: '100 allow multiple ports to be opened'
firewall::ports: [22]
firewall::proto: tcp
firewall::action: accept


classes:
  - epel
  - role::puppetmaster